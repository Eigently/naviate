#!/usr/bin/env bash

# Setup SSH keys
command -v ssh-agent >/dev/null || ( apt-get update -y && apt-get install openssh-client -y )
eval $(ssh-agent -s)

# The B64 private key can be generated by the following command: cat ~/.ssh/key_name | base64 -w0
echo $API_SERVER_SSH_PRIVATE_KEY_B64 | base64 -d | tr -d '\r' | ssh-add -
mkdir -p ~/.ssh
chmod 700 ~/.ssh
ssh-keyscan -t rsa $API_SERVER >> ~/.ssh/known_hosts

# Deploy docker image
cat d_atis.dockerimage | bzip2 | ssh root@$API_SERVER "bunzip2 | docker load"
ssh root@$API_SERVER "dokku tags:create naviate-d-atis previous; dokku tags:deploy naviate-d-atis latest"
